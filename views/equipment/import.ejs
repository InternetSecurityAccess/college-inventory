<%- include('../partials/header') %>

<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0">Импорт оборудования из CSV</h5>
            </div>
            <div class="card-body">
                <div class="alert alert-info">
                    <h6><i class="fas fa-info-circle"></i> Инструкция по импорту:</h6>
                    <ul class="mb-0">
                        <li>Файл должен быть в формате CSV с разделителем-запятой</li>
                        <li>Обязательные поля: <code>type_name</code>, <code>inventory_number</code></li>
                        <li>Дополнительные поля: <code>serial_number</code>, <code>classroom_name</code>, <code>status</code>, <code>purchase_date</code>, <code>comment</code></li>
                        <li>Статус может быть: <code>active</code>, <code>broken</code>, <code>repair</code>, <code>written_off</code></li>
                        <li>Дата покупки в формате: <code>YYYY-MM-DD</code></li>
                    </ul>
                </div>

                <form method="POST" action="/equipment/import" enctype="multipart/form-data">
                    <div class="mb-3">
                        <label for="csv_file" class="form-label">CSV файл *</label>
                        <input type="file" class="form-control" id="csv_file" name="csv_file" accept=".csv" required>
                    </div>
                    
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-file-import"></i> Импортировать
                        </button>
                        <a href="/equipment" class="btn btn-secondary">Отмена</a>
                        <a href="/equipment/export" class="btn btn-outline-info">
                            <i class="fas fa-download"></i> Скачать шаблон
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<%- include('../partials/footer') %>